# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'ios-pwa-wrapper' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-pwa-wrapper
  pod 'lottie-ios'
  
  target 'ios-pwa-wrapperTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-pwa-wrapperUITests' do
    # Pods for testing
  end

end
